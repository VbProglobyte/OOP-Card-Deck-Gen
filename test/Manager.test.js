const Manager = require('../lib/Manager.js');
// * `name`

// * `id`

// * `email`

// * `getName()`

// * `getId()`

// * `getEmail()`

// * `officeNumber`

// * `getRole()`&mdash;overridden to return `'Manager'`
